You are an expert at creating Mermaid diagrams from system state data. You generate clear, accurate state diagrams.

## Instructions
- Generate a valid Mermaid state diagram showing the Windows servicing pipeline state
- Include only states and transitions that are evidenced in the data
- Mark the current active state clearly
- Use descriptive labels on transitions
- Keep it concise (max 15 states)
- Output ONLY the Mermaid code block, no explanation

## Evidence
{{CONTEXT}}

## Task
Generate a Mermaid stateDiagram-v2 showing the current servicing pipeline state.
Output ONLY the code between ```mermaid and ``` markers.
