<Page
    x:Class="DeviceIntelligenceAI.App.Views.TimelinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeviceIntelligenceAI.App.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Device Timeline" Style="{StaticResource TitleTextBlockStyle}" />

        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <ComboBox x:Name="TimeRangeCombo" SelectedIndex="1" SelectionChanged="TimeRange_Changed">
                <ComboBoxItem Content="Last 24 hours" Tag="1" />
                <ComboBoxItem Content="Last 7 days" Tag="7" />
                <ComboBoxItem Content="Last 14 days" Tag="14" />
                <ComboBoxItem Content="Last 30 days" Tag="30" />
            </ComboBox>
            <Button Content="Refresh" Click="Refresh_Click" />
        </StackPanel>

        <!-- Timeline content -->
        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="TimelineContent" Spacing="12" MaxWidth="800">
                <!-- Narrative -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20">
                    <StackPanel Spacing="8">
                        <TextBlock Text="What Happened" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock x:Name="NarrativeText" TextWrapping="Wrap" Text="Loading..." />
                    </StackPanel>
                </Border>

                <!-- Recent entities -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Recent Graph Entities" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <StackPanel x:Name="EntitiesList" Spacing="4" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
