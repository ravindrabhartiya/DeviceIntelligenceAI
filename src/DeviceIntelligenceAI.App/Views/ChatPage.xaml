<Page
    x:Class="DeviceIntelligenceAI.App.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeviceIntelligenceAI.App.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Ask Your Device" Style="{StaticResource TitleTextBlockStyle}" />

        <!-- Chat History -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer">
            <StackPanel x:Name="ChatHistory" Spacing="12" MaxWidth="800" />
        </ScrollViewer>

        <!-- Input -->
        <Grid Grid.Row="2" ColumnSpacing="8" MaxWidth="800">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryInput"
                     Grid.Column="0"
                     PlaceholderText="Ask a question about your device..."
                     KeyDown="QueryInput_KeyDown" />
            <Button x:Name="SendButton"
                    Grid.Column="1"
                    Content="Ask"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="Send_Click" />
        </Grid>
    </Grid>
</Page>
